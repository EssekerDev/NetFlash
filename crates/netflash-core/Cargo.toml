[package]
name = "netflash-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
description = "Pure WAN quality engine for NetFlash (probes, score, hysteresis, color)."

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"

[dependencies]
